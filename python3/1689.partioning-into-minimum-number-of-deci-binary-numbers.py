class Solution:
    def minPartitions(self, n: str) -> int:
        # maxNum = 0
        # for num in str(n):
        #     if int(num) > maxNum:
        #       maxNum = int(num)
        # return maxNum


        # return max([int(num) for num in str(n)])
        return max(n)
            